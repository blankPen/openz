{
  "name": "uran",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "build": "pnpm -r run build"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.178"
  }
}
